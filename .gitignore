# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcresult
DerivedData/
build/

# glyph-auth-bridge (web)
glyph-auth-bridge/node_modules/
glyph-auth-bridge/dist/

# Secrets — never commit real keys
.env
.env.*
*.xcconfig.local
BAYCClubhouse/Secrets.plist

# Build artifacts
glyph-auth-bridge/tsconfig.tsbuildinfo

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
